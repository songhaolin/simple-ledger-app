spring:
  # MongoDB 测试配置
  data:
    mongodb:
      host: localhost
      port: 27017
      database: ledger_test

# JWT 配置（测试环境）
jwt:
  secret: ThisIsAVeryLongSecretKeyForTestingJWTTokenGenerationHS512Algorithm512BitsMinimum
  expiration: 86400000  # 24小时，单位：毫秒

# 日志配置（测试环境）
logging:
  level:
    com.ledger: DEBUG
    org.springframework.data.mongodb: DEBUG
